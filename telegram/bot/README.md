# Telegram Bot

This folder is the official workspace for the Telegram bot client layer.

Current runtime implementation still lives inside `cloudflare-worker/` because the Cloudflare Worker handles the deployed Telegram webhook.

Do not move deployed Worker files here until imports, routes, Wrangler config, and deployment paths are updated together.
